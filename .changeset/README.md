# Changesets

Каталог хранит «дельты» для следующего релиза. Добавляй файл с описанием изменения перед мержем в `main`:

```bash
bun x @changesets/cli add
```

См. https://github.com/changesets/changesets
